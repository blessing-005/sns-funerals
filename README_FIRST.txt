SNS Funerals — FINAL PRODUCTION-READY REVIEW PROJECT

1. Extract this ZIP.
2. Open the project folder in VS Code.
3. In PowerShell run:
   Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass -Force
   Get-ChildItem -Recurse -Filter *.ps1 | Unblock-File
   .\DEPLOY.ps1

Repository identity: sns-funerals
Review mode uses noindex,nofollow.
