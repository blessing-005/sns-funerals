
const m=document.querySelector('.menu-btn'),n=document.querySelector('.nav');if(m&&n){m.addEventListener('click',()=>{const o=n.classList.toggle('open');m.setAttribute('aria-expanded',String(o));m.textContent=o?'×':'☰'});n.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>n.classList.remove('open')))}
const obs='IntersectionObserver'in window?new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');obs.unobserve(e.target)}}),{threshold:.08}):null;document.querySelectorAll('.reveal').forEach(e=>obs?obs.observe(e):e.classList.add('in'));
const items=[...document.querySelectorAll('.gallery-item')],lb=document.querySelector('.lightbox');let idx=0;if(lb&&items.length){const img=lb.querySelector('img');const show=i=>{idx=(i+items.length)%items.length;img.src=items[idx].dataset.full;lb.classList.add('open');lb.setAttribute('aria-hidden','false');document.body.style.overflow='hidden'};const close=()=>{lb.classList.remove('open');lb.setAttribute('aria-hidden','true');document.body.style.overflow=''};items.forEach((x,i)=>x.addEventListener('click',()=>show(i)));lb.querySelector('.lb-close').onclick=close;lb.querySelector('.lb-prev').onclick=()=>show(idx-1);lb.querySelector('.lb-next').onclick=()=>show(idx+1);lb.addEventListener('click',e=>{if(e.target===lb)close()});document.addEventListener('keydown',e=>{if(!lb.classList.contains('open'))return;if(e.key==='Escape')close();if(e.key==='ArrowLeft')show(idx-1);if(e.key==='ArrowRight')show(idx+1)});let sx=0;lb.addEventListener('touchstart',e=>sx=e.changedTouches[0].clientX,{passive:true});lb.addEventListener('touchend',e=>{const dx=e.changedTouches[0].clientX-sx;if(Math.abs(dx)>50)show(idx+(dx<0?1:-1))},{passive:true})}
